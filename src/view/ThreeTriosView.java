package view;

import model.ReadOnlyGameModel;

public class ThreeTriosView {
  private final ReadOnlyGameModel model;

  public ThreeTriosView(ReadOnlyGameModel model) {
    this.model = model;
    // Set up GUI components here
  }

  public void setVisible(boolean visible) {
    // Code to make your GUI visible, e.g., frame.setVisible(visible);
  }

  // Other methods for rendering and updating the view
}
